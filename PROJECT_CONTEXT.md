# 人工智能实验箱 - 总上下文

更新时间：2026-09-01

## 作用

这是整个项目的总入口，只保留最少的全局信息。

## 规则

- 只在需要时再读对应子项目的 `PROJECT_CONTEXT.md`。
- 不要默认加载整棵 `G:\codexwork\人工智能实验箱`。
- 不要把历史日志、会话、模型清单、资源清单全部当成默认上下文。
- 涉及语音、机械臂、视觉、云台时，先定位到对应子项目，再读该子项目文档。

## 当前结构

- `子项目/`：每个软件一个独立子项目。
- `子项目/共享模块与资源/`：公共代码、测试脚本、动作配置、清单。
- `子项目/RK3588_语音识别_机械臂/`：RK3588 最新运行快照与总览。
- `资料/`：外部资料、接线图、说明书等。

## 任务原则

- 普通任务只读当前相关子项目。
- 公共模块只在确实共享时读取。
- 日志、历史、快照、manifest 只按需读取。
- 不要重复复述已经写在子项目文档里的细节。
